// Crear una petición reutiliza el formulario genérico de "nuevo proceso", pero
// bajo /peticiones para que el sidebar resalte "Peticiones" (no "Procesos"). El
// tipo llega por ?tipo=ID y el form queda pre-bloqueado en esa modalidad.
export { default } from "@/app/(dashboard)/procesos/nuevo/page";
