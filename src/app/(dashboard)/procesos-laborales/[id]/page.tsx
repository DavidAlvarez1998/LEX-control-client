// La ficha de un proceso laboral reutiliza la pantalla de detalle de proceso,
// bajo /procesos-laborales para que el sidebar resalte la sección. Mismo
// componente y datos.
export { default } from "@/app/(dashboard)/procesos/[id]/page";
