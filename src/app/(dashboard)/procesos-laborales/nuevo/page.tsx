// Crear un proceso laboral reutiliza el formulario genérico de "nuevo proceso",
// bajo /procesos-laborales para que el sidebar resalte esa sección. El tipo llega
// por ?tipo=ID y el form queda pre-bloqueado (rol + instancia se eligen dentro).
export { default } from "@/app/(dashboard)/procesos/nuevo/page";
